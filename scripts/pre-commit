#!/bin/sh
# pre-commit Hook v2
# Upgraded to run via pre-commit framework

if ! command -v pre-commit >/dev/null 2>&1; then
    echo "⚠ pre-commit not found. Installing..."
    pip3 install pre-commit
fi

pre-commit run --files $(git diff --name-only HEAD)
